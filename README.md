# Olin Design Principles

## Install

* Install Ruby.
* Install bundler: `gem install bundler`
* `bundle install`

## Develop

`bundle exec jekyll serve`

## Deploy

`bundle exec jekyll build && netlify deploy`
